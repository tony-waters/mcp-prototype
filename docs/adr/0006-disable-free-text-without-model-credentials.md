# Disable Free Text Without Model Credentials

When no model provider credentials are configured, the free-text assistant UI will show a clear unavailable state and disable submission. This avoids implying that AI answers can be produced without a configured provider, while preserving the default local demo and test path without paid model calls.
