# Bundle One Default Model Provider

The free-text assistant slice will include one bundled model provider as the default runnable path while keeping application code behind provider-neutral Spring AI abstractions. This gives the prototype a concrete live-model setup without spreading provider-specific decisions through the application design or carrying multiple provider integrations before they are needed.
