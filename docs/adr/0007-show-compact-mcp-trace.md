# Show a Compact MCP Trace

The free-text assistant UI will show a compact MCP trace by default: tool name, arguments, status, and a short result summary. This gives the Support Agent evidence for the answer without turning the workspace into a raw protocol debugger.
