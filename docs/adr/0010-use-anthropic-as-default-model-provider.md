# Use Anthropic as the Default Model Provider

The free-text assistant slice will bundle Anthropic as the default live-model provider while keeping application code behind provider-neutral Spring AI abstractions. This gives the demo one concrete hosted provider path without making the core agent workflow Anthropic-specific.
